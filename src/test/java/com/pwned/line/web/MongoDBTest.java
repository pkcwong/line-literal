/*
package com.pwned.line.web;

import org.junit.Test;

import static org.junit.Assert.*;

public class MongoDBTest {

	@Test
	public void getHost() throws Exception {
		assertEquals("ds113046.mlab.com", new MongoDB(System.getenv("MONGODB_URI")).getHost());
	}

	@Test
	public void getPort() throws Exception {
		assertEquals("13046", new MongoDB(System.getenv("MONGODB_URI")).getPort());
	}

	@Test
	public void getUser() throws Exception {
		assertEquals("user", new MongoDB(System.getenv("MONGODB_URI")).getUser());
	}

	@Test
	public void getPassword() throws Exception {
		assertEquals("password", new MongoDB(System.getenv("MONGODB_URI")).getPassword());
	}

	@Test
	public void getDatabase() throws Exception {
		assertEquals("line-literal-test", new MongoDB(System.getenv("MONGODB_URI")).getDatabase());
	}

}
*/